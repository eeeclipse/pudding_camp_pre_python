## 과제-20240314-1


```
class mpti_test() : 
    def __init__(self, 문항데이터 경로):
        ## 초기화
        print("MPTI 검사를 시작합니다.")
        데이터 = 문항데이터 읽어오기
        self.데이터 = 데이터

    def 검사기 시작(self) : 
        
        데이터 = self.데이터 

        입력값 저장 딕셔너리 = {
            "m" : {
                "m1" : 0,
                "m2" : 0,
            }, 
            "p" : {
                "p1" : 0,
                "p2" : 0,
            }, 
            "t" : {
                "t1" : 0,
                "t2" : 0,
            }, 
            "i" : {
                "i1" : 0,
                "i2" : 0,
            }, 
        }


    def 검사 진행()
        
        데이터 = self.데이터 
        
        ## todo : 질문 랜덤출력
        
        데이터 있는 동안 : 
            입력값 = 사용자 입력 
            입력값이 답변 범위에 있으면 :
                결과 업데이트, 입력값에 해당하는 중첩 딕셔너리의 값을 +1
                다음 질문 출력
            입력값이 답변 범위에 없으면 : 
                print(입력 요청)
            입력값이 "q" 이면 : 
                검사 종료
        
        Return 입력값 저장 딕셔너리 

    def 검사 종료() : 
        검사결과 = 검사 진행()
        최종결과 문자열 생성 = ""

        딕셔너리 키 갯수만큼 반복 :
            중첩 딕셔너리 값 대소비교  
            값이 더 큰 키를 최종결과 문자열에 합치기   
        
        print(f"MPTI 검사결과 {최종결과 문자열} 입니다.")

if __name__ == "__main__" :
    mpti_test(문항데이터 경로)

```